<div class="calendar-container">
  <div class="calendar-toolbar">
    <div class="calendar-current-period">
      <h2>{{calendar.periodName}}</h2>
    </div>
    
    <div class="calendar-nav-container">
      <div class="calendar-view-selector">
        <select id="calendar-view-dropdown" class="calendar-view-dropdown">
          <option value="month" {{#equals calendar.viewType "month"}}selected{{/equals}}>{{t "month_view"}}</option>
          <option value="week" {{#equals calendar.viewType "week"}}selected{{/equals}}>{{t "week_view"}}</option>
          <option value="workweek" {{#equals calendar.viewType "workweek"}}selected{{/equals}}>{{t "workweek_view"}}</option>
          <option value="day" {{#equals calendar.viewType "day"}}selected{{/equals}}>{{t "day_view"}}</option>
        </select>
      </div>
      
      <div class="calendar-navigation">
        <button class="calendar-prev" aria-label="{{t 'previous'}}">
          <span class="icon">◀</span>
        </button>
        <button class="calendar-today" aria-label="{{t 'today'}}">
          {{t 'today'}}
        </button>
        <button class="calendar-next" aria-label="{{t 'next'}}">
          <span class="icon">▶</span>
        </button>
      </div>
    </div>
  </div>
  {{!-- Vista diaria --}}
  {{#equals calendar.viewType "day"}}
  <div class="calendar-day-view">
    <div class="calendar-day-container">
      <div class="calendar-day-main-view">
        <div class="calendar-day-header">
          <div class="calendar-day-column-header {{#if calendar.isToday}}calendar-today{{/if}}">
            <div class="calendar-day-name">{{calendar.dayName}}</div>
            <div class="calendar-date">{{getDayOfMonth calendar.date}}</div>
          </div>
        </div>
        <div class="calendar-day-body">
          <div class="calendar-day-full-row">
            <div class="calendar-day-column {{#if calendar.isToday}}calendar-today{{/if}}" data-date="{{toISODate calendar.date}}">
              {{#each calendar.tasksForDay}}
                <div class="calendar-task" 
                    data-file-path="{{file.path}}" 
                    data-line-number="{{line.number}}"
                    data-tooltip="{{section.desc}}">
                  <span class="task-title">{{section.desc}}</span>
                </div>
              {{/each}}
            </div>
          </div>
        </div>
      </div>

      <div class="calendar-mini-sidebar">
        <div class="calendar-mini-month">
          <div class="calendar-mini-month-header">
            <div class="mini-month-nav">
              <button class="mini-calendar-prev" aria-label="{{t 'previous_month'}}">
                <span class="icon">◀</span>
              </button>
              <div class="mini-month-name">{{calendar.miniCalendar.monthName}}</div>
              <button class="mini-calendar-next" aria-label="{{t 'next_month'}}">
                <span class="icon">▶</span>
              </button>
            </div>
          </div>
          <div class="calendar-mini-month-weekdays">
            {{#each calendar.miniCalendar.weekdays}}
              <div class="calendar-mini-weekday">{{this}}</div>
            {{/each}}
          </div>
          <div class="calendar-mini-month-grid">
            {{#each calendar.miniCalendar.weeks}}
              <div class="calendar-mini-week">
                {{#each this}}
                  <div class="calendar-mini-day {{#if isCurrentMonth}}current-month{{else}}other-month{{/if}} {{#if isToday}}mini-today{{/if}} {{#if isSelected}}mini-selected{{/if}}" 
                       data-date="{{date}}">
                    <div class="mini-day-content {{#if hasTasks}}has-tasks{{/if}}">
                      {{day}}
                    </div>
                  </div>
                {{/each}}
              </div>
            {{/each}}
          </div>
        </div>
      </div>
    </div>
  </div>
  {{/equals}}
</div>